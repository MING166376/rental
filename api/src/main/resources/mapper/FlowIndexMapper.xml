<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.kmbeast.mapper.FlowIndexMapper">

    <select id="list" resultMap="BaseResultMap">
        SELECT fi.id, fi.type, fi.times, fi.content_type, fi.content_id, fi.user_id, fi.create_time
        FROM flow_index fi
        <where>
            <if test="type != null">
                AND fi.type = #{type}
            </if>

            <if test="userId != null">
                AND fi.user_id = #{userId}
            </if>

            <if test="contentType != null and contentType != ''">
                AND fi.content_type = #{contentType}
            </if>

            <if test="contentId != null">
                AND fi.content_id = #{contentId}
            </if>

        </where>

        <if test="current != null and size != null">
            LIMIT #{current},#{size}
        </if>

    </select>

    <!--满足分页条件的数据总项-->
    <select id="listCount" resultType="integer">

        SELECT COUNT(*)
        FROM flow_index fi
        <where>
            <if test="type != null">
                AND fi.type = #{type}
            </if>

            <if test="userId != null">
                AND fi.user_id = #{userId}
            </if>

            <if test="contentType != null and contentType != ''">
                AND fi.content_type = #{contentType}
            </if>

            <if test="contentId != null">
                AND fi.content_id = #{contentId}
            </if>

        </where>

    </select>

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.kmbeast.pojo.entity.FlowIndex">
        <id column="id" property="id"/>
        <result column="user_id" property="userId"/>
        <result column="type" property="type"/>
        <result column="times" property="times"/>
        <result column="content_type" property="contentType"/>
        <result column="content_id" property="contentId"/>
        <result column="create_time" property="createTime"/>
    </resultMap>

</mapper>
